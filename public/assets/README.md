# Public Assets Directory
Upload your store logo here as `logo.png`, `logo.jpg`, or `logo.svg`.
The online receipt and invoice generator will automatically detect and display it at the top of receipts.
